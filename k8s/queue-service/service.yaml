apiVersion: v1
kind: Service
metadata:
  name: queue-service
  namespace: klinik-antrian
  labels:
    app: queue-service
spec:
  type: ClusterIP
  ports:
    - port: 5004
      targetPort: 5004
      protocol: TCP
  selector:
    app: queue-service
