apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: klinik-antrian
  labels:
    app: patient-service
spec:
  type: ClusterIP
  ports:
    - port: 5003
      targetPort: 5003
      protocol: TCP
  selector:
    app: patient-service
