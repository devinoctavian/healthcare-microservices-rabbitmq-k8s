apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: klinik-antrian
  labels:
    app: mysql
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  selector:
    app: mysql
  clusterIP: None  # Headless service for StatefulSet
