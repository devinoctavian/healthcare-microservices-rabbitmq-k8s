apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: klinik-antrian
  labels:
    app: auth-service
spec:
  type: ClusterIP
  ports:
    - port: 5001
      targetPort: 5001
      protocol: TCP
  selector:
    app: auth-service
